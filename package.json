{
  "name": "tts-cli",
  "version": "0.1.0",
  "description": "Markdown to MP3 CLI Tool",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile tts-cli",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@clack/prompts": "^0.7.0",
    "@types/cli-progress": "^3.11.6",
    "citty": "^0.1.6",
    "cli-progress": "^3.12.0",
    "smol-toml": "^1.3.0"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
