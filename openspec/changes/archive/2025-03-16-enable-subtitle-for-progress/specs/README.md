# Specs

此变更不涉及 spec 层面的需求变更。

## Reason

进度条功能已在现有 specs 中隐含定义（作为 `tts-convert` 和 `tts-play` 的一部分）。此变更仅修复实现细节以使进度条正常工作，不改变用户可见的行为契约。

## Verification

进度条的平滑更新可以通过以下方式验证：
- 运行 `tts-cli <input>` 并观察进度条是否显示中间值（而非直接跳到 100%）
- 使用包含多个句子的输入文件（如测试文档）验证进度更新
